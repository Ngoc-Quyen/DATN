<!DOCTYPE html>
<html lang="vi">

<head>
    <title>Đăng ký tài khoản mới</title>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <!-- Bootstrap 4.3, Jquery 3.3, font-awesome 4.7, slick-slider-->
    <script src="/components/jquery/dist/jquery.min.js"></script>
    <link rel="stylesheet" href="/components/bootstrap/css/bootstrap.min.css">
    <link rel="stylesheet" href="/css/style.css">
    <link rel="stylesheet" href="/css/auth.css">
    <link rel="stylesheet" href="/components/font-awesome/css/font-awesome.min.css">
    <script src="/components/bootstrap/js/bootstrap.min.js"></script>
    <script src="/js/auth.js"></script>
</head>

<body>
    <div class="container">
        <div class="row min-vh-100 align-items-center justify-content-center py-4">
            <div class="col-lg-10 col-xl-9">
                <div class="card shadow-lg border-0 rounded-4 overflow-hidden">
                    <div class="row g-0">
                        <div class="col-md-6 d-none d-md-block register-img-bg">
                            <!-- Background image is set via CSS -->
                            <img src="../images/background/IMG_Register.png" alt="Login Image"
                                class="img-fluid rounded-start-4 object-fit-cover">
                        </div>
                        <div class="col-md-6">
                            <div class="card-body p-4 p-lg-5">
                                <h3 class="fw-bold mb-4">Đăng ký tài khoản mới</h3>

                                <% if(locals.hasErrors){ %>
                                    <div class="alert alert-danger" role="alert">
                                        Vui lòng kiểm tra lại thông tin
                                    </div>
                                    <% } %>

                                        <form method="POST" action="/register">
                                            <div class="mb-3">
                                                <label for="name" class="form-label">Họ và tên: <span
                                                        class="text-danger">*</span></label>
                                                <input type="text" class="form-control rounded-3" id="name" name="name"
                                                    value="<%= locals.oldData?.name || '' %>" required />
                                            </div>

                                            <div class="mb-3">
                                                <label for="email" class="form-label">Email: <span
                                                        class="text-danger">*</span></label>
                                                <input type="email" class="form-control rounded-3" id="email"
                                                    name="email" value="<%= locals.oldData?.email || '' %>" required />
                                                <% if (locals.errEmail) { %>
                                                    <small class="form-text text-danger">
                                                        <%= locals.errEmail %>
                                                    </small>
                                                    <% } %>
                                            </div>

                                            <div class="mb-3">
                                                <label for="password" class="form-label">Mật khẩu: <span
                                                        class="text-danger">*</span></label>
                                                <input type="password" class="form-control rounded-3" id="password"
                                                    name="password" required />
                                                <% if (locals.errPassword) { %>
                                                    <small class="form-text text-danger">
                                                        <%= locals.errPassword %>
                                                    </small>
                                                    <% } %>
                                            </div>

                                            <div class="mb-3">
                                                <label for="rg-password-again" class="form-label">Nhập lại mật
                                                    khẩu: <span class="text-danger">*</span></label>
                                                <input type="password" class="form-control rounded-3"
                                                    id="rg-password-again" name="passwordConfirm" required />
                                                <% if (locals.errPasswordConfirm) { %>
                                                    <small class="form-text text-danger">
                                                        <%= locals.errPasswordConfirm %>
                                                    </small>
                                                    <% } %>
                                            </div>

                                            <div class="mb-3">
                                                <label for="phone" class="form-label">Số điện thoại:</label>
                                                <input type="text" class="form-control rounded-3" id="phone"
                                                    name="phone" />
                                            </div>

                                            <div class="mb-3">
                                                <label for="birthday" class="form-label">Ngày sinh:</label>
                                                <input type="date" class="form-control rounded-3" id="birthday"
                                                    name="birthday" />
                                            </div>

                                            <p class="text-muted small">(<span class="text-danger">*</span>) là trường
                                                bắt buộc</p>
                                            <button type="submit"
                                                class="btn btn-primary w-100 fw-bold rounded-pill py-2 mt-2">ĐĂNG
                                                KÝ</button>
                                        </form>

                                        <hr class="my-4">

                                        <div class="text-center">
                                            <p class="mb-2">Đã có tài khoản?
                                                <a href="/login"
                                                    class="link-primary fw-semibold text-decoration-none">Đăng
                                                    nhập</a>
                                            </p>
                                            <a href="/" class="text-secondary text-decoration-none small">
                                                <i class="fa fa-arrow-left me-1"></i> Về lại trang chủ
                                            </a>
                                        </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</body>

</html>
